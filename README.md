#### JSX (React) playground. Interactive JSX compiling in browser.

Tech stack:
- Bootstrap (CSS only)
- React
- Gulp

In a console run:

    $ git clone https://github.com/AlexKVal/interactive-jsx
    $ cd interactive-jsx
    $ npm install -g gulp
    $ npm install
    $ gulp
